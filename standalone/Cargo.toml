[package]
name = "standalone"
version = "0.1.0"
edition = "2021"

[dependencies]
ecs_hybrid = { path = "../engine" }
libloading = "0.8"
notify = "6"
macroquad = { version = "0.4", optional = true }

[features]
rendering = ["dep:macroquad", "ecs_hybrid/rendering"]
