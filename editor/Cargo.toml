[package]
name = "editor"
version = "0.1.0"
edition = "2021"

[dependencies]
host = { path = "../host", features = ["rendering"] }
dioxus = { version = "0.7", features = ["desktop"] }
futures-util = "0.3"
tokio = { version = "1", features = ["rt"] }
