[package]
name = "pill_core_macros"
version = "0.1.0"
edition = "2021"
description = "Procedural macro implementing the Pill semantic error DSL (#[engine_error])."

[lints]
workspace = true

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full", "extra-traits"] }
