[package]
name = "game"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ecs_hybrid = { path = "../engine" }
trait_type_map = { path = "../../trait_type_map" }
serde = { version = "1", features = ["derive"] }
